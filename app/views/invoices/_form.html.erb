<%= bootstrap_form_for :invoice, :url => invoices_path, :html => { :class => :form } do |f| %>
  <% if @invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% @invoice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :comment, :class => "label" %><br />
    <%= f.text_area :comment %>

    <%= f.label :price, :class => "label" %><br />
    <%= f.text_field :price %>

    <%= f.label :date, :class => "label" %><br />
    <%= f.date_select :date %>

    <%= f.label :creator_id, :class => "label" %><br />
    <%= f.number_field :creator_id %>
  <div class="actions">
    <%= f.submit :class => "btn primary" %>
    <%= link_to 'Cancel', invoices_path %>
  </div>
<% end %>
