<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h2>Moin <%= @user.name %></h2>
    <p>
      <%= @invoice.creator.name %> hat eine Rechnung erstellt, die dich betrifft:
    </p>
    <p>
      <b>Gesamtpreis:</b> <%= euro @invoice.price %> <br>
      <b>Schuldner:</b> <%= @invoice.users.map { |user| user.name }.join(', ') %> <br>
      <b>Kommentar:</b> <br>
      <%= simple_format @invoice.comment %>
    </p>
    <p>
      Dein aktueller Kontostand liegt jetzt bei <b><%= euro @user.standing %></b> <br>
      <%= link_to 'Online anschauen', user_url(@user, :host => 'wg-tool.herokuapp.com') %>
    </p>
  </body>
</html>